import { PaymentVerificationPayload } from './payment-verification-payload';

describe('PaymentVerificationPayload', () => {
  it('should create an instance', () => {
    expect(new PaymentVerificationPayload()).toBeTruthy();
  });
});
